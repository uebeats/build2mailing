/**
 * En body.js puedes ingresar información que puede ser usada en la plantilla
 * accediendo a traves de un ciclo for() a items[].
 * Contenido de ejemplo:
 * photo: inserta la ruta de la imagen require().
 * link: inserta la URL para direccionar al usuario cuando haga clic
 * en la imagen.
 */

let body = {
  items: [
    
    {
      // photo: require(''),
      link: 'https://webpack.org'
    },

    {
      // photo: require(''),
      link: 'https://webpack.org'
    },

  ]

}

module.exports = body;